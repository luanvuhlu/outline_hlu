# -*- coding: utf-8 -*-
from __future__ import unicode_literals

# from models import CurrentWeek

# Weekly
def incre_week():
	"""Đặt lại tuần hiện tại cho các môn 15 tuần và môn 5 tuần
	"""
	# current_week_obj_list=CurrentWeek.objects.all()[:1]
	# if not current_week_obj_list:
	# 	logger.debug(u'Không tìm thấy thông tin đối tượng Tuần hiện tại')
	# 	return
	# current_week_obj = current_week_obj_list[0]
    # current_week = datetime.now().isocalendar()[1]
    # old_current_wwek = current_week_obj.start_date.isocalendar()[1]
    # logger.debug(u'Tuần hiện tại %s, Tuần cũ %s' % (current_week, old_current_wwek))
    # if current_week_obj.off_next_week:
    #     logger.debug(u'Tuần sau được nghỉ')
    # if current_week_obj.off_next_week or current_week - old_current_wwek != 1:
    #     logger.debug(u'Không thay đổi tuần hiện tại')
    #     return
    # current_week_obj.current_week_15 += 1
    # current_week_obj.current_week_5 += 1
    # current_week_obj.save()
    pass
